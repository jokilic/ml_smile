name: ml_smile
description: Showcasing Google ML Kit

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.9.4+5
  confetti: ^0.6.0
  cupertino_icons: ^1.0.4
  get: ^4.6.1
  google_ml_kit:
    git:
      url: https://github.com/bharat-biradar/Google-Ml-Kit-plugin
  path_provider: ^2.0.8
 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
